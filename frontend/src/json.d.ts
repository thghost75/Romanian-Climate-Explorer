// Data is generated from the verified archive during deployment. Permit checking
// the source checkout before the multi-gigabyte climate snapshot is installed.
declare module '*.json' {
 const value: {stations:unknown;baseline:string;firstYear:number;lastYear:number;snapshotDate:string};
 export default value;
}
